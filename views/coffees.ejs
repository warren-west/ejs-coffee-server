<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Coffee Catalog</title>
    <%- include('./partials/stylingHead.ejs') %>
    <script src="/jquery.min.js"></script>
    <script src="/js/coffees.js" defer></script>
</head>
<body>
   <%- include('./partials/navbar.ejs') %>
   <%- include('./partials/header.ejs') %>

    <form action="/coffees" method="get" class="container">
        <div class="form-group">
            <label for="coffee-search">Search: </label>
            <input id="coffee-search" type="text" placeholder="Java" name="search">

            <button class="btn btn-primary" type="submit">Search</button>
        </div>
    </form>

    <div class="container">
        <% for( let i = 0; i < coffees.length; i++ ) { %>
            <div class="coffee-container">
                <h3>
                    <%= `(${coffees[i].id}) ${coffees[i].description}` %>
                    <% if (viewedCoffees.includes(coffees[i].id)) { %>
                        <span>👁️👁️</span>
                    <% } %>   
                </h3>
                <p><%= `${coffees[i].price} kr.` %></p>
                <% if ( currentUser ) { %>
                    <a href="<%=`/coffees/${coffees[i].id}`%>">View Coffee Details</a>
                <% } else { %>
                    <a href="/login">Login to View Coffee Details</a>
                <% }  %>
            </div>
        <% } %>

    </div>

</body>
</html> 